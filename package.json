{
  "name": "upr",
  "version": "1.0.0",
  "description": "UPR server + React dashboard (Docker/Render)",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:init": "psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -f ./db/init.sql",
    "seed": "node scripts/ensure-seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^6.1.0",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.16.3"
  }
}
